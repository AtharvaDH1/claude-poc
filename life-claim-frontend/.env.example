# Production: leave empty — backend serves app + /api on same host (3010)
VITE_API_URL=
# Dev only: Vite proxies /api to this target
VITE_PROXY_TARGET=https://192.168.60.62:3010
VITE_KEYCLOAK_URL=http://localhost:8081
VITE_IDLE_TIMEOUT_MINUTES=5
